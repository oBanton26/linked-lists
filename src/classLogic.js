export class LinkedList {
    constructor (head = null) {
        this.head = head;
    }
};

export class Node {
    constructor (value, nextNode) {
        this.value = value;
        this.nextNode = nextNode;
    };
    
}